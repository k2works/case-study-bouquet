spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/frere}
    driver-class-name: org.postgresql.Driver
    username: ${DB_USERNAME:frere}
    password: ${DB_PASSWORD:frere}
  flyway:
    enabled: true
    locations: classpath:db/migration
