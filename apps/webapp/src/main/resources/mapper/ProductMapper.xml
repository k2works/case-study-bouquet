<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.frere.infrastructure.persistence.mybatis.ProductMapper">

    <resultMap id="productResultMap" type="com.example.frere.domain.model.product.Product">
        <id property="productId" column="product_id"/>
        <result property="productName" column="product_name"/>
        <result property="description" column="description"/>
        <result property="price" column="price"/>
        <result property="isActive" column="is_active"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findAll" resultMap="productResultMap">
        SELECT product_id, product_name, description, price, is_active, created_at, updated_at
        FROM products
    </select>

    <select id="findById" resultMap="productResultMap">
        SELECT product_id, product_name, description, price, is_active, created_at, updated_at
        FROM products
        WHERE product_id = #{productId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="productId">
        INSERT INTO products (product_name, description, price, is_active)
        VALUES (#{productName}, #{description}, #{price}, #{isActive})
    </insert>

    <update id="update">
        UPDATE products
        SET product_name = #{productName},
            description = #{description},
            price = #{price},
            is_active = #{isActive},
            updated_at = CURRENT_TIMESTAMP
        WHERE product_id = #{productId}
    </update>

    <delete id="deleteById">
        DELETE FROM products WHERE product_id = #{productId}
    </delete>

</mapper>
